# Kata exercise generator for zig

## Set Up

Run
```sh
zig build-exe init.zig
```

Then:
```
./init
```

The init command will create the exercies in the exercises directory. Currently, init overwrites existing exercises.
